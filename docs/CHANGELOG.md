
# 변경 이력

## 2026-09-13
- 대표 URL을 `https://dankookaims.org`로 통일하고 canonical·Open Graph·Twitter 이미지·구조화 데이터에 커스텀 도메인을 적용
- 네이버·일반 검색로봇이 읽을 수 있는 동적 `robots.txt`, `sitemap.xml`, `rss.xml`을 추가하고 공지·사진 상세 URL을 사이트맵에 포함
- 주요 화면과 공지·사진 상세의 서버 응답에 경로별 제목·설명·canonical을 제공하고 관리자 화면과 404 응답은 `noindex` 처리
- 존재하지 않는 경로를 실제 404로 응답하고 기존 호환 경로는 대표 경로로 308 이동하며 Render 기본 도메인은 커스텀 도메인으로 통합
- 네이버 소유확인 토큰을 `NAVER_SITE_VERIFICATION` 배포환경 변수로 안전하게 주입할 수 있도록 구성하고 SEO 스모크 테스트 추가

## 2026-09-08
- 학술대회·저널 논문 등록과 수정에 PDF·DOC·DOCX·HWP·HWPX·PPT·PPTX·JPG·JPEG·PNG·WebP 첨부파일을 최대 5개, 파일당 10MB, 논문당 합계 30MB까지 추가하고 확장자·MIME·파일 시그니처를 함께 검증
- 논문 첨부 등록·추가·교체·삭제는 활성 관리자에게만 허용하고, 공개 목록·상세 JSON에는 바이너리를 제외한 메타데이터만 제공하며 실제 파일은 안전한 공개 다운로드 응답에서만 전달
- 논문 첨부 원본을 기존 Neon PostgreSQL `paper_attachments.file_data`의 `BYTEA`에 영구 저장하고 논문 삭제 시 함께 정리해 Render 로컬 디스크 재시작·재배포와 무관하게 유지
- 논문 등록·수정과 사진 앨범 등록·이미지 추가 창에 키보드 파일 선택과 마우스 드래그 앤 드롭을 함께 지원하는 업로드 영역을 적용하고, 선택 파일·용량·제거·제한 오류 상태를 화면에 표시
- 성균관대학교 기술경영전문대학원 사진자료실의 정보 밀도와 탐색 구조를 참고한 공개 사진자료실 목록(`/photos`)과 상세(`/photos/:id`) 화면 추가
- 사진 앨범 목록에 제목·게시일·조회수·사진 수와 대표 사진을 표시하고, 제목·내용 검색·페이지 이동·빈 상태·로딩·오류 상태를 반응형 화면으로 제공하며 게시기관과 본문은 상세에서 안내
- 상세 화면에 4:3 주 이미지, 이전·다음 사진, 썸네일 선택, 자동 재생 일시정지, 이전글·다음글·목록 이동과 개별 이미지·전체 ZIP 다운로드 추가
- 사진 등록·정보 수정·이미지 추가·순서 변경·이미지 삭제·앨범 삭제 조작은 관리자 로그인 상태에서만 표시하고, 모든 변경 API에서도 활성 관리자 권한을 재검증하도록 구성
- 업로드된 JPEG·PNG·WebP의 확장자·MIME·파일 시그니처를 확인한 뒤 Sharp로 방향을 보정하고 최대 2000×1500 WebP로 최적화해 기존 Neon PostgreSQL의 `BYTEA`에 영구 저장
- 사진 업로드를 요청당·앨범당 최대 12개·입력 파일당 8MB·최적화 결과 파일당 3MB·앨범당 30MB로 제한하고, 서버 전체에서 한 번에 한 요청만 multipart 버퍼링·최적화하도록 해 메모리 급증을 방지
- 전체 ZIP 생성을 서버 동시 2건으로 제한하고, 화면 표시용 이미지와 다운로드에 각각 15분당 600회·60회의 별도 제한을 적용하며 ETag 기반 즉시 재검증으로 삭제 사진의 장기 캐시 잔존을 방지
- 공개 사진·다운로드·앨범 ZIP 응답은 Neon 데이터에서 제공해 Render 로컬 디스크 재시작·재배포와 무관하게 유지되도록 분리
- 목록은 데스크톱 3열·좁은 화면 2열로 전환하고, 설명형 대체텍스트·가시적 키보드 포커스·현재 썸네일 상태·동작 감소 설정을 반영

## 2026-08-31
- `/admin` 관리자 로그인 화면과 운영 진입 경로를 복원하고, 서버에서 scrypt 해시가 검증된 활성 관리자만 인증되도록 구성
- 공지사항·논문(학술대회·저널)·입학안내·모집요강은 누구나 조회할 수 있도록 유지하되 등록·수정·삭제 버튼과 관련 API는 활성 관리자에게만 허용
- 최초 활성 관리자가 없을 때만 일회성 설정 화면을 제공하고, 관리자 활성화 후에는 해당 화면을 숨겨 아이디·비밀번호만으로 일반 로그인하도록 정리
- 공지·논문 첨부 업로드를 활성 관리자에게 한정해 비로그인 방문자의 고아 파일 생성을 차단하고, 등록된 첨부 다운로드는 공개 열람을 유지
- 관리자 비밀번호는 마스킹 입력과 TLS 전송을 사용하며 평문·해시·실제 계정 식별자가 Git 이력, 클라이언트 번들, 운영 문서와 요청 로그에 남지 않도록 유지
- 홈 바로가기와 모바일 상단 메뉴의 내부 경로를 선택하면 학과 소개·공지사항·논문·학과 내규·입학안내 화면의 최상단으로 이동하도록 보완
- 메인 히어로 제목을 좁은 모바일에서 31–35px 유동 크기로 조정하고 `데이터로 지식을 만들고,`를 한 줄로 유지해 Galaxy Z Fold6 줄바꿈을 보완
- `직장인을 위한 토요일 전일 수업`의 테두리·배경을 제거하고 핵심어 Accent 색상과 텍스트 그림자로 강조

## 2026-08-24
- 박사과정 컨택 헤더에 표준 졸업모 아이콘과 메일 아이콘을 추가하고 제목·본문 타이포그래피를 소폭 확대했으며, 359px 이하에서는 아이콘·영문 머리말 다음 행에 한글 제목을 배치하고 `지도교수에게 이메일 보내기` 앞에는 숫자나 목록 마커가 표시되지 않도록 유지
- 학과소개 지도교수 영역을 최대 1440px 래퍼의 단일 카드로 확장하고 데스크톱에서 사진 전용 좌측 패널과 교수 정보·연구분야 우측 패널을 약 1:3 비율로 배치하며 하단 여백을 축소
- 박사과정 컨택 안내를 지도교수 카드 바로 아래 전체 폭 독립 카드로 배치하고 단계·번호 표기를 제거한 확정 문구와 확대된 타이포그래피를 반영
- 공지·논문·모집요강 등록·수정·삭제를 로그인 없이 공개하고 동일 출처·입력 검증·속도 제한과 삭제 확인을 유지
- 공개 공지 첨부 업로드·다운로드를 허용하되 확장자·MIME·파일 시그니처·개수·용량과 요청 속도 제한을 유지
- 논문 댓글 수·목록·작성 UI를 제거하고 댓글 쓰기 API를 410으로 종료하되 기존 데이터·스키마는 보존
- 헤더의 관리자 로그인·로그아웃·관리자 메뉴를 제거하고 `/admin`을 모집요강으로 연결하며 서버 시작 시 불필요한 bootstrap 코드 로그 생성을 중지
- 논문 사이트 주소를 선택 항목으로 정리해 URL이 없는 논문도 등록·수정할 수 있게 하고 클라이언트 쓰기 DTO를 서버 입력 계약에 맞춤
- 실제 Express·세션·동일 출처 미들웨어와 메모리 저장소를 연결한 HTTP 회귀검사로 공개 CRUD, 논문 댓글 410과 잘못된 Origin 403을 검증
- 지도교수 사진의 상단 간격을 넓혀 `지도교수`·교수명을 사진 위에, 소속·AIMS Lab을 사진 아래에 명확히 배치
- 박사과정 컨택 제목과 첫 안내 문장을 간결한 운영 문구로 정리하고 이메일 전송 조작을 굵은 밑줄 링크로 제공
- 관리자용 공지·논문 등록·수정 창을 제출 가능한 폼으로 보완해 Enter 저장을 지원하고, 중복 요청을 막는 저장 상태와 44px 수정·삭제 조작 영역을 적용
- 관리자 허브에서 입학안내·모집요강 관리 경로를 명확히 표시하고 논문 삭제 실패를 화면에 알리도록 보완
- 공지·논문·모집요강과 관리자 계정 삭제 중 중복 요청을 차단하고, 확인 버튼 상태·44px 조작 영역·낮은 화면의 모달 스크롤을 보완
- 공지 첨부 선택을 키보드로 접근 가능하게 바꾸고 최대 5개·파일당 10MB 제한을 선택 단계에서 안내·검증
- 박사과정 컨택의 이메일 문의·회신 안내·기타 문의와 자유양식 PDF 작성방식을 확정 문안과 일치시키고 모든 이메일을 `mailto:` 링크로 유지
- 모집요강 검색을 전체·제목·내용·게시기관 범위 선택, 키보드 Enter 제출, 실제 검색 버튼과 결과 상태 알림을 갖춘 접근 가능한 폼으로 변경
- 모집요강 최신 게시일 정렬을 클라이언트·메모리·PostgreSQL에서 일치시키고 긴 제목·기관명 및 모바일 검색 컨트롤의 가로 넘침을 방지
- 관리자 해시 생성 명령은 대화형 TTY 숨김 입력만 허용하고, Render 빌드는 lockfile 기반 `npm ci --legacy-peer-deps`로 통일
- GitHub Actions와 Render 런타임을 Node.js 24로 맞추고 `main` 검사 통과 커밋 자동 배포를 명시

## 2026-08-23
- 학과 소개 지도교수 영역 아래의 인재풀 개인정보 입력 양식을 제거하고 박사과정 사전 컨택 이메일 안내로 교체
- 헤더·홈·푸터의 `인재풀 등록`을 `입학안내 > 모집요강`으로 변경하고 기존 `/talent-pool` 주소를 입학안내 화면으로 호환 연결
- 게시 기관·날짜·조회수·HTTPS 첨부 링크를 제공하는 모집요강 목록, 검색, 상세와 관리자 등록·수정·삭제 기능 추가
- 새 모집요강 테이블이 비어 있는 최초 배포에 단국대학교 공식 모집요강·입학안내 링크 3건을 기본 등록
- 방문자 개인정보 입력·동의 UI, 공개 회원가입과 개인정보처리방침 링크를 제거하고 관련 공개 API를 종료
- 최초 활성 관리자가 없을 때 Render 로그의 15분 유효 일회성 코드로 관리자 계정을 안전하게 만드는 무환경변수 초기 설정 절차 추가
- Render Blueprint에서 관리자 아이디·비밀번호 해시 수동 환경 변수 입력 항목을 제거
- 관리자 추가·비밀번호 변경·삭제 화면과 API를 추가하고 현재 계정·마지막 활성 관리자 삭제를 서버에서 차단
- 정상 scrypt 비밀번호로 로그인 가능한 활성 관리자만 최초 설정·마지막 관리자 보호 기준에 포함하고, 최초 생성 경쟁조건을 DB 잠금으로 차단
- 관리자 비밀번호 변경 시 인증 버전을 증가시켜 기존 세션을 모두 무효화하고 현재 계정은 즉시 재로그인하도록 변경
- 관리자 로그인 창을 닫을 때 입력 비밀번호·오류·표시 상태를 즉시 초기화
- 모바일 관리자 수정·삭제 버튼을 항상 표시하고 키보드 포커스 시에도 보이도록 접근성 개선
- 모바일 전체 메뉴에 접근 가능한 제목을 추가하고 관리자 화면에 공지·논문·모집요강 관리 바로가기 제공
- 모바일 하위 메뉴에 현재 페이지 색상과 `aria-current` 상태를 표시
- 공지 제목을 키보드로 열 수 있도록 버튼 의미와 포커스 스타일을 적용
- 모집요강 날짜를 한국시간 기준 ISO 형식으로 통일해 최신순 정렬과 자정 시간대 기본 날짜 오류를 수정
- Neon 데이터베이스 TLS 인증서 검증을 활성화
- 모집요강 조회수 증가를 단일 `UPDATE ... RETURNING` 연산으로 처리해 동시 삭제와의 경쟁조건 제거
- 동시 관리자 아이디 등록 충돌을 409로 처리하고 관리자 목록 쿼리에서 비밀번호·이메일 컬럼을 읽지 않도록 최소화
- 관리자 최초 설정·추가·비밀번호 변경을 키보드 Enter로 제출할 수 있게 하고 비밀번호 확인 입력을 추가
- 모집요강 제목 버튼의 HTML 구조와 키보드 포커스 스타일을 바로잡고 긴 관리자 아이디 줄바꿈을 보완
- 공지·논문·모집요강·파일 업로드의 변경 권한을 활성 관리자에게 한정하고 각 요청마다 데이터베이스 계정 상태를 재검증
- 관리자 비밀번호는 scrypt 해시로만 저장하고 관리자 응답에서 비밀번호·해시·이메일을 제외
- 저장소 파일은 삭제하지 않았으며 오래된 파일 삭제 후보는 별도 승인 후 처리하도록 분리

## 2026-08-21
- Render에 별도 세션 환경 변수를 추가하지 않아도 384비트 임시 난수로 안전하게 기동하도록 하고, 신규 Blueprint에는 256비트 세션 비밀값 자동 생성을 적용
- 브라우저 코드에 노출된 관리자 인증값을 제거하고 scrypt 해시 기반 서버 인증으로 전환
- 회원·인재풀·업로드·공지/논문 변경·댓글 변경 API에 인증, 역할 및 소유권 검사를 적용
- 기존 평문 비밀번호를 배포 시 자동 무효화하고 신규·재설정 비밀번호를 scrypt로 저장
- PostgreSQL 영속 세션, 로그인 세션 재발급, 30분 유휴/8시간 절대 만료 및 안전한 쿠키 정책 적용
- 동일 출처 검증, 요청 속도 제한, 입력 길이·형식 검증, 민감 API 캐시 금지 적용
- API 요청/응답 본문 로깅을 제거하고 요청 식별자 기반 최소 로그 및 관리자 감사 이벤트로 교체
- 업로드 크기·개수·확장자·MIME·파일 시그니처를 검증하고 인증 사용자만 다운로드하도록 변경
- CSP·HSTS·클릭재킹·MIME 스니핑·브라우저 권한 방지 헤더와 개인정보 동의/보유기한 기록 추가
- 저장소 문서의 운영 접속정보·고정 세션 비밀값을 제거하고 미사용 브라우저 localStorage 인증 코드를 삭제
- 호환 범위 내 의존성 보안 업데이트와 Drizzle ORM 보안 버전 상향을 적용하고 고위험 의존성 감사 게이트 추가
- 프로덕션 빌드 중 데이터베이스 스키마를 자동 변경하던 동작을 제거하고 마이그레이션 도구를 개발 의존성으로 격리

## 2026-08-19
- 지도교수 연구관심분야를 상단 4열 2행과 하단 2개 대형 카드 구조로 개편
- 지도교수 소속 아래 AIMS Lab 영문 명칭을 추가하고 학과 개요 문장을 요청 문구와 줄바꿈으로 정리
- 브라우저 탭 제목을 `단국대학교 대학원 데이터지식서비스공학과`로 통일하고 DKU 심볼 파비콘의 식별성을 개선
- 논문 목록의 연도 배지를 조회수로 교체하고 상세 열람마다 조회수가 원자적으로 1씩 증가하도록 개선
- 학과 내규의 장식용 이모지를 장 번호 중심 구성으로 교체하고 논문·공지사항의 파일 이모지를 표준 선형 아이콘으로 통일
- 헤더 학과명 아래에 `AIMS Lab(에임즈 랩) : AI, Innovation, Metaverse & Service Lab` 표기 추가
- 학과 소개 설명의 마지막 문장을 줄바꿈하고 지도교수 연구관심분야 10개 항목을 정리
- 메인 히어로 영문 표기를 `DATA SCIENCE`로 간결화

## 2026-08-18
- 논문 분류를 `학술대회`와 `저널` 두 항목으로 통합하고 기존 국내·해외·본 심사 자료가 새 분류에 자동으로 모이도록 호환 처리
- 논문 등록·수정 입력 항목을 논문 제목, 저자, 사이트 주소로 단순화하고 URL 형식 검증 추가
- 학과 소개 상단 이미지 바로 아래에 지도교수 영역을 먼저 배치하고, 기존 학과 개요·전공 소개·교과과정이 뒤따르도록 순서 변경
- 서응교 지도교수 연구관심분야를 M&A·PMI, e-business 인터페이스, CMC, IT management, Design thinking, CSCL, MOOC 등 10개 항목으로 확장
- 학과 소개 페이지 지도교수 영역을 사진·교수 정보 2열 카드로 개편
- 서응교 지도교수 사진, 연구관심분야(Artificial Intelligence, Metaverse, Smart City), 이메일 링크 추가
- 학과 소개, 공지사항, 학과 내규, 논문, 인재풀 등록 페이지의 상단 배경을 사용자 제공 단국대학교 이미지로 순차 교체
- 5개 페이지가 동일한 높이·타이포그래피·오버레이·반응형 크롭 규칙을 공유하도록 `PageHero` 컴포넌트 추가
- 이미지별 피사체 위치에 맞춰 `object-position`을 조정하고 짙은 Navy 오버레이로 텍스트 명암비 확보
- 원본 PNG를 웹용 JPEG로 최적화해 배경 이미지 총 용량을 약 8.8MB에서 약 0.9MB로 축소

## 2026-08-17
- 메인 히어로를 단국대학교 죽전캠퍼스 이미지와 확정 문구로 개편
- “학과 소개 보기”, “인재풀 등록” 핵심 행동 버튼 재배치
- 공공 교육기관형 2단 헤더와 반응형 모바일 메뉴 적용
- 최신 공지·자료실 2열 목록, 6개 빠른 메뉴, 학과 교육 분야 구획 구성
- 단국대학교 로고 기반 Open Graph 이미지와 파비콘으로 Replit 기본 자산 교체
- 색상·타이포그래피·4px 간격·6px 버튼·10px 카드 규칙 문서화
- 프로덕션 `SESSION_SECRET` 필수화 및 기본 보안 응답 헤더 추가
- 프로젝트 명세, 테스트 계획, 작업 규칙 문서 추가
- 격리된 Vite UI 미리보기와 전체 Express 개발 서버 실행 명령 분리
- 메모리 저장소의 논문 연도 값을 스키마 타입과 일치하도록 정규화
- 데이터베이스 URL이 없는 로컬 빌드에서는 스키마 푸시를 안전하게 건너뛰도록 빌드 절차 보완
